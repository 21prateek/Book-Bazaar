// 1.
export const status = {
  PENDING: "pending",
  PAID: "paid",
  FAILED: "failed",
};

export const allStatus = Object.values(status);

// 2.
export const availableRoles = {
  USER: "USER",
  ADMIN: "ADMIN",
};

export const allRoles = Object.values(availableRoles);
